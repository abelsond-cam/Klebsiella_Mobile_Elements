# Env for Snakemake and all pipeline rules. Managed by you (e.g. micromamba); pipeline does not use --use-conda.
# Must contain: snakemake, bwa, bowtie2, mgefinder (and deps for run_pipeline if you run it from this env).
name: mgefinder_env